apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-info-chart.fullname" . }}-headless

spec:
  clusterIP: None

  selector:
    app: devops-info

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}